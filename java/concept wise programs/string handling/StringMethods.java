public class StringMethods {
    public static void main(String args[])
    {
        //1)))))))))
        //length of a string
        String s="hansi";
        System.out.println(s.length());
        //or
        System.out.println("hansi".length());//String object is created for every string literal, you can use a string literal any place you can use a String object.
        //array length
        int array2[]={1,2,3,4};
        System.out.println("length of arary= "+array2.length);

        //))))))))

        }
    }

